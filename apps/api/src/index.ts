// TODO(M2): Hono app——auth/nation/world/build/policy/market/military/diplomacy/messages/rankings/tasks 路由。
// 薄殼:驗 session → 組 ctx → 呼叫純模塊 → 寫 D1。錯誤格式統一 { error: string } + 4xx。

export function placeholder(): string {
  return 'apps/api scaffold — TODO(M2)';
}
