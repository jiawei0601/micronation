# 複製成 .env.local 使用(不進版控)。
# 預設走真 API(/api/*,見 apps/api)。要在後端未啟動/未實作完的情況下用假資料開發前端,
# 取消下一行註解並設成 1:
# VITE_USE_MOCK=1

# 真 API 的 base path,預設 /api(走同源反代)。要指到別的位置(例如本機另開 port 的 wrangler dev)
# 才需要設這個。
# VITE_API_BASE=http://localhost:8787/api
