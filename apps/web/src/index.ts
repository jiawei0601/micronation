// TODO(M2+): 路由 / /nation /build /policy /market /military /diplomacy /rankings /tasks /treaty/:id。
// 只依賴 shared(型別+預覽公式)與 HTTP API,輪詢 /api/world 每 45s。字串集中 src/i18n/zh-Hant.ts。

export function placeholder(): string {
  return 'apps/web scaffold — TODO(M2+)';
}
